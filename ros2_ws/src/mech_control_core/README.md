# mech_control_core

Vendor-independent C++17 core package boundary. Foundation v0.1 starts with a
small build/test marker; frame, time, configuration, transport and runtime
contracts follow without introducing ROS headers into this public API.
